import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "TBMate",
  description:
    "TBMate adalah aplikasi inovatif yang membantu pasien tuberkulosis memantau jadwal minum obat dengan disiplin dan mudah. Dengan fitur verifikasi AI, TBMate memastikan setiap dosis diminum tepat waktu, memberikan ketenangan pikiran bagi pasien dan tenaga medis.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
